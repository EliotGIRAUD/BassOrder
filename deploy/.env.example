# Copier vers deploy/.env sur le VPS (ne jamais committer)
# openssl rand -hex 32
BASSORDER_JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS

# Pool knowledge (anti-empoisonnement) — optionnel
# BASSORDER_POOL_MIN_VOTES=2
# BASSORDER_LIKES_CAP=100
