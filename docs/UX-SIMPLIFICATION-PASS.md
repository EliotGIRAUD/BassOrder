# Plan — Simplifications UX (passe complète)

Décisions : **tout d’un coup** · Terrain orbital → **Réglages / FX**.

## Ordre

1. **Accueil** — `hint={false}` sur PushEq hero ; ticker masqué si Mode Perf
2. **Mes fichiers** — CTA primaire = **Deviner les genres** ; Classer / Importer en secondaire
3. **Hist. local** — carte compacte (titre, %, titres, 3 tags, Rouvrir)
4. **Spotify** — copy « compte » ; Compléter genres = seul `btn-accent`
5. **Hist. Spotify** — carte « ACTIF » en tête ; le reste dans `<details>`
6. **Mon espace** — légende Toi vs Spotify ; retirer orbital
7. **Dictionnaire** — guide fermé après 1× (`localStorage`) ; lede « explorer »
8. **Réglages** — Langue + Perf en tête ; FX dans `<details>` ; orbital optionnel en bas FX

## Hors scope

- Quota Spotify, PIN prompt, refonte DB, CI Mac
